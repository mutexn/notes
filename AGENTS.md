# AGENTS.md

このリポジトリで作業する際の指示。

## 概要

個人的な学習ノートと、その公開用サイト。`app/` 配下を静的サイトとして Cloudflare Pages が配信する。ビルドツールもパッケージマネージャも使っておらず、HTML を直接配信する構成。依存パッケージは追加しない。

ディレクトリ構成と Cloudflare Pages の設定は [README.md](README.md) を参照。

## app/ の扱い

- ディレクトリ名がそのまま URL のパスになる。ページを追加するときは `app/<page-name>/index.html` を作る
- 子ページは `app/<page-name>/<child-name>/index.html` のように入れ子にする
- 各ページは単一の HTML ファイルで完結させる。CSS は `<style>` に、必要なら JS も同じファイルに含める
- **内部リンクは末尾スラッシュ付きで書く**（`/page-name/`）。付けないと 308 リダイレクトが挟まる

ページを追加したら、目次（`app/index.html`）にもリンクを追加する。目次は `.p-grid` のカードグリッドで、1 ページ 1 カード。分野やカテゴリによる見出しは設けない（全てのページに分類を与えられるとは限らないため）。

カードは DADS の card コンポーネントに準拠し、次で構成する。

- **ラベル**（`.p-card__label`）— 形式と分量（例: 読み物・全9章）
- **タイトル**（`.p-card__title`）— リンク。DADS の規定によりアンダーラインを必ず付ける
- **説明文**（`.p-card__desc`）— 2〜3 行程度
- **サブエリア**（`.p-card__sub`）— 子ページがある場合のみ。リンクリストを置く

カードのコンテナは必ず背景色と外周をもち、外周は外側の背景に対して 3:1 以上のコントラストを確保する（`--gray-420`）。影だけで境界を表現してはいけない。雛形は `app/index.html` にコメントで残してある。

Cloudflare Pages の挙動で注意すべき点。

- **存在しないパスには `/index.html` が HTTP 200 で返る**。`404.html` は置かない方針のため、Cloudflare Pages のフォールバックが働く。ファイルの有無を応答コードで判断できないので、存在確認をするときは中身まで見る
- ディレクトリ形式のページは、末尾スラッシュなしの URL から末尾スラッシュありへ 308 リダイレクトされる

## 自動生成された HTML の扱い

`app/` 配下には、Marp などのツールが出力した HTML が含まれることがある。元のソース（Markdown 等）はリポジトリに無く、**再生成できない**。次を守ること。

- **生成部分の `<style>` は直接編集しない**
- スタイルを調整するときは、生成された CSS の直後に `<style data-dads-override="true">` ブロックを挿入し、カスケードの後勝ちで上書きする
- 生成物のセレクタは詳細度が高く、独特な形をしていることがある（例: `div#\:\$p > svg > foreignObject > section`）。上書きするときは既存のセレクタをそのままコピーして使う。詳細度がずれると効かない
- 配色が CSS 変数で一元管理されている場合は、個別の色指定ではなく変数を差し替える

Marp のプレゼン出力には構造上の制約がある。

- スライドを SVG の `foreignObject` 内に固定サイズ（1280×720px）で描画し、画面幅に合わせて縮小表示する。そのため画面が狭いほど実効フォントサイズが小さくなる。この制約は CSS では解決できない
- `overflow: hidden` が効いているため、フォントサイズを上げると溢れた分が読めなくなる。サイズを変更したら全スライドの表示を必ず目視で確認する

## reference/ の扱い

外部資料。参照専用で、**内容を書き換えない**。出典表記の義務があるため、ディレクトリごと削除したり README の出典記載を消したりしない。

## デザイン規約（DADS）

新しく作るページはデジタル庁デザインシステムに準拠させる。カラー値の出典は [@digital-go-jp/design-tokens](https://github.com/digital-go-jp/design-tokens) v2.0.1（MIT）。

| 用途 | トークン | 値 | 対白コントラスト |
| --- | --- | --- | --- |
| 本文 | solid-gray-900 | `#1a1a1a` | 17.40:1 |
| 副文 | solid-gray-800 | `#333333` | 12.63:1 |
| 補助テキスト | solid-gray-536 | `#767676` | 4.54:1 |
| 罫線 | solid-gray-420 | `#949494` | 3.03:1 |
| 背景の淡色 | solid-gray-50 | `#f2f2f2` | — |
| リンク | blue-900 | `#0017c1` | 11.10:1 |
| 訪問済リンク | magenta-900 | `#8b008b` | 8.50:1 |
| 押下中リンク | orange-900 | `#ac3e00` | 6.10:1 |
| フォーカス | yellow-300 | `#ffd43d` | Black との 2 重構造（変更禁止） |

タイポグラフィ:

- 書体は Noto Sans JP、等幅は Noto Sans Mono
- **14 px 未満は使わない**。本文・UI は 16 px 以上
- 行高は本文 160〜175%、見出し 140〜150%、表など高密度用途は 130%
- 字間は 16〜26 px で 0.02em、32 px 前後で 0.01em、45 px 以上で 0

守るべき下限:

- テキストと背景のコントラスト比 **4.5:1 以上**
- 罫線・境界線など非テキスト要素は **3:1 以上**
- リンクは色だけで示さず、必ず下線など色以外の手段を併用する

色を変更したら、実際にコントラスト比を計算して上記を満たすことを確認する。

過去に作ったページには独自デザインのものもある。既存ページを変更するときは、DADS を一律に適用せず、対象ページの既存スタイルを確認してから判断する。

## 作業の進め方

- 変更後は `npx wrangler pages dev app` で起動し、ブラウザで表示を確認する（http://127.0.0.1:8788/）
- 大きな HTML ファイル（数百 KB になることがある）を編集するときは、全体を書き直さず、スクリプトで該当箇所だけを置換する

## コミット

- コミットメッセージは英語で書く（body に日本語の説明を追加してもよい）
- author は `~/.gitconfig` の設定に従う。`-c user.email` や `--author` での上書きはしない
- `main` への直接の force push はしない
